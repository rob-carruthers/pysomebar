 # pysomebar

Status updater for `somebar`, `dwlb`, or dwl with `bar` and `barcolor` patches applied.

Use `dwlb` bar type for both dwlb and dwl with bar/barcolor patches.

Colours are supported for all bars except `somebar`.
